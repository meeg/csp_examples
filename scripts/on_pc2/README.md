# files to copy to the PC2
These files need to be copied to the `/data/fram` and `/data/nandp0v1` partitions on the PC2.

In addition, the LTA daemon needs to be statically compiled on a similar CPU (PYNQ-Z2 works) and copied to `/data/nandp0v1/configure.static`.
